# Hannah Lim Portfolio

Static portfolio site showcasing selected work across applied AI, full-stack product development, data systems, and community technology.

## Local preview

Run a static server in this directory, for example:

```powershell
python -m http.server 4173
```

Then open `http://127.0.0.1:4173`.
